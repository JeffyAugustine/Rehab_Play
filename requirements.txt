# Requires Python 3.10 (mandated by Isaac Sim and Isaac GR00T; other versions unsupported)
#
# Core RL / simulation stack (installed via Isaac Lab environment; listed for reference)
# isaaclab and isaacsim are provided by the Isaac Lab Launchable / local install,
# not installed via pip directly. See docs/setup.md.

skrl>=1.3.0
gymnasium>=0.29.0

# Dashboard
streamlit>=1.38.0
pandas>=2.2.0
plotly>=5.24.0

# Utilities
numpy>=1.26.0
pyyaml>=6.0.2
